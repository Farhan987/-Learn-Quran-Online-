class MyRequest {
  constructor(studentId, teacherId, requestStatus) {
    this.student = studentId;
    this.teacher = teacherId;
    this.requestStatus = requestStatus;
  }
}
export default MyRequest;
